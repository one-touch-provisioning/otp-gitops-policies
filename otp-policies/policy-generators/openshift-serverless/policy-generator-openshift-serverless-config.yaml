apiVersion: policy.open-cluster-management.io/v1
kind: PolicyGenerator
metadata:
  name: install-openshift-serverless
policyDefaults:
  namespace: rhacm-policies
  placement:
    placementRulePath: placementrules
  remediationAction: enforce
policies:
  - name: install-openshift-serverless
    manifests:
      - path: subscriptions
